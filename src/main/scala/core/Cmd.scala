package pers.cyz.bubble
package core

object Cmd {
  def parsing(args: String): Unit = {
    val cmdArray: Array[String] = args.split("\\s+")
    cmdArray.foreach(x => {
      x match {
        case
      }
    })
    println(cmdArray)
  }
}
